{
    // Shoddy's blocks are indentation-based (a tab counts as 4) and the
    // whole corpus uses 4 spaces — keep it that way.
    "[shoddy]": {
        "editor.tabSize": 4,
        "editor.insertSpaces": true,
        "editor.detectIndentation": false
    }
}
