Rem Copyright (c) 2026 Stephen Vincent Foster. All rights reserved.
Rem
Rem This file is part of the Shoddy Language project.
Rem Licensed under the Shoddy Language License 1.0.0 (PolyForm Noncommercial
Rem License 1.0.0 with Additional Use Grant). See the LICENSE file in the
Rem project root for full terms.

Rem ==================================================================
Rem file.shoddy — Shoddy standard library: text files, line-oriented
Rem Built on the whole-file builtins (ReadFile WriteFile AppendFile
Rem FileExists DeleteFile) plus str.shoddy. Normalizes \r\n on read.
Rem File words are effectful, like Print/Input: keep them at the edges.
Rem ==================================================================

Include "str.shoddy"

Def ReadLines(path As String) As List Of String
    Let c = Replace(ReadFile(path), "\r", "")
    If c = "" Then
        { }
    Else
        If EndsWith(c, "\n") Then
            Split(Left(c, Len(c) - 1), "\n")
        Else
            Split(c, "\n")

Def WriteLines(path As String, xs As List Of String)
    WriteFile(path, Join(xs, "\n") & "\n")

Def AppendLine(path As String, s As String)
    AppendFile(path, s & "\n")
