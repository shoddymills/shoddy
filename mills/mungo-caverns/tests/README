= Testing Mungo Caverns =

Five suites and a harness, all but the last needing nothing but the
mill:

  ../test.shoddy        the generator, the table sizes, the parser, and
                        the output channel.  The fast one.
  test-tables.shoddy    what must be true of the cave itself: every
                        travel row leads somewhere real, every block of
                        rules ends in a stop, every room you can stand in
                        describes itself, every hint costs points, and
                        which rooms the travel table does not connect.
  test-turn.shoddy      whole turns, driven headless.  A turn is
                        Command(Game, String) -> Done, so a scenario is a
                        function call and not a recorded session: take
                        the lamp, stand in the dark, unlock the grate,
                        suspend and resume, and ask the game what it did.
  test-walk.shoddy      one game played from the road to the gold and
                        back, with the score checked at every landmark.
                        Thirteen commands through the parser, the travel
                        table, two forced rooms, the darkness rule, the
                        lamp and the scorer: one wrong number anywhere
                        and the walk lands somewhere else.
  test-fuzz.shoddy      450 commands drawn at random from the game's own
                        vocabulary, checking only that the game is still
                        a game after each one.  Deterministic: the
                        commands come from the same generator, run from
                        a separate seed threaded alongside the game.
  harness.shoddy        the game, plus the twelve lines of turn loop the
                        suites above need.  They include this and
                        nothing else.
  run.sh                the C original's recorded transcripts, replayed.
                        The port's actual specification -- and the only
                        layer that needs anything from outside the repo.

    ./build.sh test     runs everything but run.sh   (~20 seconds)
    ./build.sh smoke    16 recorded transcripts      (~3 minutes)
    ./build.sh check    all 107                      (~18 minutes)

Everything but run.sh is a baseline: it cannot tell you the port is
faithful to the original, only that it still does what it did yesterday
and that the cave is internally coherent.  The transcripts are the
oracle, and where the two disagree the transcripts win.  Keep both.

Two notes on running them.  The suites are run with their input closed,
because dying asks whether you want to be resurrected and that is a
read; with no input the answer is no, which is why a fuzz run reports
how far it got.  And test-turn.shoddy writes and re-reads one save file,
tests/roundtrip.adv, which is how the save format's round trip is
checked; it is the only thing here that touches the disk, and it is
gitignored.


= The transcript harness =

mungo-caverns is a port, so its specification is not a description of what
the game should do -- it is a recording of what the original does.  The
C original ships ~107 of those recordings in its own tests/ directory,
each a .log of commands paired with the .chk of exact output they
produce.  This directory replays them against mungo-caverns.

Ninety-two of the transcripts open with an in-band "seed" command that
pins the linear congruential generator.  That is what makes them worth
more than golden files snapshotted from the current build: a change that
draws one extra random number, or skips one draw, diverges every seeded
game from that point onward and is caught here.  A golden file taken
from today's mungo-caverns would happily bless the divergence.

== Running ==

    ./run.sh                 all transcripts        (~18 minutes)
    ./run.sh --smoke         16 transcripts         (~3 minutes)
    ./run.sh axebear pirate_carry
    ./run.sh --keep ...      keep the work directory

Or, from the mill directory, "build.cmd check" / "./build.sh check" for
the full sweep and "build.cmd smoke" / "./build.sh smoke" for the
subset.  Runtime is dominated by mill startup, about ten seconds per
transcript, so the subset is the one to use between edits and the full
sweep the one to use before calling a change finished.

Set OPENADV if the C source tree is not beside the repository.  The
default is ../../../../open-adventure-master.

== The three adjustments ==

mungo-caverns renamed the cave's little knife-throwing folk to
curmudgeons.  The rename is total -- the upstream noun, its plural and
its adjective map onto curmudgeon, curmudgeons and curmudgeonish, and
nothing else moved -- so run.sh substitutes in both directions.  It has
to be both: the output names them, but so does the input, because
several transcripts type commands at them by name and the port's own
vocabulary no longer holds that word.  The two sed filters in run.sh
(to_curmudgeon and to_upstream) are the only place the old spelling
survives here; they run longest-form-first, or the plural comes back
mangled.

The game itself was also renamed to Mungo Caverns, and the old names
appear in recorded output: the welcome line, the cave the game says is
nearby, the suspend and resume messages, the version line.  Two old
names collapse into the one new one, so this rename cannot be undone
from the output side; run.sh maps it forward onto the expected text
instead (to_mungo), shielding "Adventurer" and "Adventuredom", which
are role words rather than the name.  Typed commands never contain the
name, so the input side needs nothing.

Because "curmudgeon" is five characters longer than the word it
replaced, and the new game name is longer than either old one, stored
message text wraps at different columns than the C's recorded output.
Both sides are therefore reflowed paragraph-by-paragraph before the diff
-- see reflow.awk, which also states what that costs.

== What the harness sets up ==

Transcripts run in a scratch directory rather than in place, because the
save tests write .adv files and the reference tree should not collect
them.  Two details of the C's layout are reproduced there:

  - badmagic feeds the game "../main.o" and expects to open a real file
    that is not a save, so a junk file sits one level above.
  - four transcripts resume from a save with one field forced to an
    impossible value.  The C writes those with a second binary, cheat;
    mungo-caverns has the same generator behind -cheat, so run.sh writes the
    fixtures itself in mungo-caverns's save format.  The two formats are not
    byte-compatible and were never intended to be.

Command-line options travel with the transcript that needs them, in a
"#options:" line the C's own Makefile reads the same way.  Three
transcripts use one: -o for the 4.30-style interface, -r to resume at
startup, -l to tee the session to a file.

== Order ==

Transcripts run in alphabetical order, as they do upstream, because
saveresume.1 through saveresume.4 are a sequence: the first suspends a
game to a file that the later ones resume.  Naming is the only thing
sequencing them.
