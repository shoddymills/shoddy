v2.0.1 fixed the toolchain path in every C# project and missed the one
F# project — the release died on its last remaining `bin\mill.exe` pin.
This patch makes the F# console proof OS-conditional like the other
eight, and ships what 2.0.0 promised: the extension, and one sparky
archive per OS. Nothing in the language changed.

## Fixed

- **The F# console proof still pinned the Windows mill.** The v2.0.1
  sweep covered `*.csproj` and `proofs/console-fs` is an `.fsproj`; its
  `ShoddyToolPath` is now OS-conditional like the rest, and the sweep
  that found it covered every project type and the MSBuild files too.
