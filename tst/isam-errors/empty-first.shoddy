Rem Copyright (c) 2026 Stephen Vincent Foster. All rights reserved.
Rem
Rem This file is part of the Shoddy Language project.
Rem Licensed under the Shoddy Language License 1.0.0 (PolyForm Noncommercial
Rem License 1.0.0 with Additional Use Grant). See the LICENSE file in the
Rem project root for full terms.

Rem ==================================================================
Rem empty-first.shoddy — expected-to-abort isam.shoddy error condition
Rem
Rem Verified by actually running it: passing means this prints exactly
Rem     ERROR (line 173): ISAMFIRST: EMPTY FILE
Rem and exits with code 1. The line number is inside machines/isam.shoddy
Rem itself (where IsamFirst's Error(...) call lives), not this file.
Rem
Rem Run: bin\mill run tst\isam-errors\empty-first.shoddy
Rem ==================================================================

Include "../../machines/isam.shoddy"

Type Kv
    K As Number
    V As String

Def RdKv(f As Number) As Kv
    Kv(GetNum(f), GetStr(f, 8))

Def WrKv(f As Number, r As Kv)
    PutNum(f, K(r))
    PutStr(f, V(r), 8)

Def KvKey(r As Kv) As Number
    K(r)

Def Main()
    If FileExists("_err_first.tmp") Then
        DeleteFile("_err_first.tmp")
    Let db = IsamOpen("_err_first.tmp", 16, RdKv, WrKv, KvKey)
    IsamFirst(db)
